<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- HTTPS-only by default: all cleartext (plain HTTP) traffic is blocked,
         which protects the production API and any image/host URLs. -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Cleartext is allowed ONLY for the local MAMP dev server on the LAN and
         the Android emulator loopback, so local testing keeps working without
         opening the whole app up to HTTP downgrade/MITM. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.39.226.168</domain>
        <domain includeSubdomains="false">192.168.0.81</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
